name: crawl (multi - no cookie)
on:
  workflow_dispatch:
    inputs:
      targets_base64:
        description: "Base64 encoded list of targets"
        required: true
jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    - name: Cache Python deps
      uses: actions/cache@v4
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-
    - name: Setup Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.21'
    - name: Cache Go modules
      uses: actions/cache@v4
      with:
        path: |
          ~/.cache/go-build
          ~/go/pkg/mod
        key: ${{ runner.os }}-go-mod-${{ hashFiles('**/go.sum') }}
        restore-keys: |
          ${{ runner.os }}-go-mod-
    - name: Cache Go binaries
      uses: actions/cache@v4
      with:
        path: ~/go/bin
        key: ${{ runner.os }}-go-bin-v2-${{ hashFiles('tools_version.txt') }}
        restore-keys: |
          ${{ runner.os }}-go-bin-
    - name: Add Go bin to PATH
      run: echo "$HOME/go/bin" >> $GITHUB_PATH
    - name: Check and install missing tools
      run: |
        cat > tools_version.txt << EOF
        katana@latest
        gospider@latest
        hakrawler@latest
        urlfinder@latest
        httpx@latest
        EOF
        check_and_install() {
          if ! command -v $1 &> /dev/null; then
            echo "Installing $1..."
            go install $2
          else
            echo "$1 already installed, skipping..."
          fi
        }
        check_and_install katana github.com/projectdiscovery/katana/cmd/katana@latest
        check_and_install gospider github.com/jaeles-project/gospider@latest
        check_and_install hakrawler github.com/hakluke/hakrawler@latest
        check_and_install urlfinder github.com/projectdiscovery/urlfinder/cmd/urlfinder@latest
        check_and_install httpx github.com/projectdiscovery/httpx/cmd/httpx@latest
    - name: Install Waymore (pip with cache)
      run: |
        if ! pip show waymore &> /dev/null; then
          echo "Installing waymore..."
          pip install waymore
        else
          echo "waymore already installed, skipping..."
        fi
    - name: Decode targets
      run: |
        echo "${{ github.event.inputs.targets_base64 }}" | base64 -d > targets.txt
    - name: Run Multi Scan
      run: python3 crawl-runner.py -l targets.txt
    - name: Extract hostname
      id: sanitize
      run: |
        TARGET="${{ github.event.inputs.target }}"
        HOSTNAME=$(echo "$TARGET" | sed -E 's|^https?://||' | sed -E 's|/.*$||')
        echo "sanitized=$HOSTNAME" >> $GITHUB_OUTPUT

    - name: Upload Result
      uses: actions/upload-artifact@v4
      with:
        name: crawl-result-${{ steps.sanitize.outputs.sanitized }}
        path: "*/"
        retention-days: 30
